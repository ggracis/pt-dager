<%- include ("partials/head", {pageTitle: "Dot Dager 🥒"}) %>

<body class="stretched">
  <!-- Document Wrapper
	============================================= -->
  <div id="wrapper">
    <%- include("partials/header-" + locals.currentLocale) %> <%-
    include("partials/form-celu-" + locals.currentLocale) %>

    <!-- Intro
		============================================= -->
    <section
      class="slider-element min-vh-60 min-vh-md-100 include-header"
      style="
        background: linear-gradient(rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.8)),
          url('/imgs/bg_2.png') no-repeat center center / cover;
      "
    >
      <div class="slider-inner">
        <div class="vertical-middle">
          <div class="container dark py-5 py-md-0">
            <div class="row justify-content-between mt-5 col-mb-50">
              <%- include ("partials/intro-" + locals.currentLocale) %> <%-
              include ("partials/skills") %>
            </div>
          </div>
        </div>
      </div>
    </section>
    <!-- #Slider end -->

    <!-- Contenido
		============================================= -->
    <section id="content" style="background-color: #000">
      <div class="content-wrap pt-0" style="overflow: visible">
        <!-- Skills -->

        <div class="clear"></div>
        <!-- Proyectos -->
        <%- include ("partials/proyectos-" + locals.currentLocale) %>
        <!-- Acerca de -->
        <div class="clear"></div>
        <%- include("partials/acerca-" + locals.currentLocale) %>

        <div class="clear"></div>

        <!-- Remate -->
        <div id="contacto">
          <%- include ("partials/remate-" + locals.currentLocale) %>
        </div>
      </div>
    </section>
    <!-- #content end -->

    <%- include ("partials/footer") %>
  </div>
</body>
